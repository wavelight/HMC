#include "motor_pwm.h"

void motor_pwm_initialize(MOTOR * motor)
{
	/* motor->pwm.set_duty	= 
	motor->pwm.get_duty	

	motor->pwm.set_freq
	motor->pwm.get_freq */
}

bool motor_pwm_set_frequency(MOTOR * motor, PWM_CHANNEL channel, uint32_t frequency)
{
	
}

uint32_t motor_pwm_get_frequency(MOTOR * motor, PWM_CHANNEL channel)
{
	
}

bool motor_pwm_set_duty(MOTOR * motor, PWM_CHANNEL channel, uint16_t duty)
{

}

uint16_t motor_pwm_get_duty(MOTOR * motor, PWM_CHANNEL channel)
{
	
}